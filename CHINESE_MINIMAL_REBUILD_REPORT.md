# GrokGUI Rebuild：中式极简六主题与逻辑链条重构报告

日期：2026-07-21

## 目标

本次改造不再依赖明显描边、重阴影、金属渐变或装饰纹理制造“高级感”，而是将六套主题重构为三组昼夜设计体系，以字体、留白、色阶、节奏和克制的中式颜色建立差异。同时重新梳理主题应用、编辑、预览、持久化、素材生命周期、设置预览与侧栏拖拽等链条。

## 六套主题

| 系列 | 昼主题 | 夜主题 | 设计语言 |
|---|---|---|---|
| 素墨 | 素笺 · 昼 | 松烟 · 夜 | 宣纸与松烟墨，温润、安静、偏阅读 |
| 天青 | 天青 · 昼 | 黛青 · 夜 | 青瓷与青黛，清冷、现代、偏工具 |
| 丹漆 | 白瓷 · 昼 | 玄漆 · 夜 | 白瓷、玄漆与低饱和朱砂，克制而有仪式感 |

原有六个主题 ID 保持不变，已有用户偏好无需迁移。

## 视觉系统改造

- 普通页面、侧栏、输入区、消息区和设置区由相邻色阶与留白建立层级，不再依赖深描边。
- 菜单、代码区、表格、键盘焦点等确实需要辨界的位置才保留轻量边界。
- 删除大面积卡片套卡片、伪玻璃高光、金属渐变、背景网格、噪点、悬浮上移和点击下压。
- 三组主题分别使用独立字体、色温、强调色和空间节奏，不再是同一套布局换色。
- 移除移动端强制统一宋体的覆盖，保证三组主题在触屏设备上仍然具有差异。
- 材质投影改为扁平、低噪声的统一语义变量，兼容合法 CSS 颜色格式和透明度。

## 逻辑链条重构

- 收束主题文档写入入口，避免 Provider、预览和系统昼夜切换竞争修改根节点。
- 区分已保存基线、当前草稿、预览主题、正式主题和系统模式，后台刷新不再覆盖脏草稿。
- Diff 与保存采用同一快照，审核后的版本不会被后续编辑偷偷替换。
- 损坏或旧版主题偏好会规范化并持久化，避免每次启动重复自愈。
- “使用整组昼夜主题”会同时配置昼夜槽位并启用系统跟随。
- 保存、导入、配置与立即切换被拆成独立阶段，提示与真实结果一致。
- 主题素材导入绑定编辑会话，异步返回不会误附着到后来打开的主题。
- 素材支持内容寻址去重、引用计数式清理、覆盖回收、删除回收和保守异常处理。
- 设置即时预览在失败或离开页面时完整回滚，不再留下临时 CSS 状态。
- 侧栏拖拽过程中仅更新本地视觉，结束时单次持久化，避免写入风暴和旧值覆盖。
- 修复字段与 CSS Token 错位，使定义的主题变量真正作用于对应组件。

## 个人应用安全策略

体验能力保持开放，包括剪贴板、全屏、画中画、自动播放、表单、下载和可信预览交互。仍保留两条低摩擦基础边界：预览 iframe 不直接获得 Electron 主进程权限；普通任务 WebSocket 不接受任意来源连接。

## 新鲜验证结果

以下验证均在最终源码上重新执行：

- `npm run typecheck`：Renderer、Server、Shell 三套 TypeScript 配置通过。
- `npm run test:segmentation`：94 / 94 通过。
- `npm run test:task-runtime`：75 / 75 通过。
- 测试总计：169 项，0 失败、0 跳过。
- `npm run architecture:boundaries`：457 个模块、1267 条依赖，0 越界。
- `npm run architecture:unused`：Knip 通过。
- `npm run architecture:duplicates`：总重复行 0.19%，重复 Token 0.13%，低于 3% 门槛。
- `npm run build`：Web、Server、Electron Shell 三端生产构建通过。

## 交付说明

ZIP 仅包含可恢复源码、配置、脚本、设计样张与本报告，不包含：

- `node_modules/`
- `dist/`、`dist-server/`、`dist-shell/`
- `release/`
- `.git/`
- 本地环境变量、运行数据或密钥

恢复方式：

```bash
npm install
npm run typecheck
npm run test:segmentation
npm run test:task-runtime
npm run build
```

## 验证边界

当前环境完成了源码静态检查、自动测试、架构门禁和三端构建。macOS 原生窗口的人工视觉验收、签名、公证和 DMG 安装链路仍需在真实 macOS 发布环境执行。
